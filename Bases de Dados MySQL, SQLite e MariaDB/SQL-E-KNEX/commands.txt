#execute no powershell dentro do vscode no .yml


docker-compose up     (usado para subir a base de dados)
docker-compose up -d  (usado para subir a base de dados mas sem poluir o terminal)
docker ps             (Ver os conteiners que estão rodando)  
docker stop cd7c      (parar conteiner)
docker start cd7c     (iniciar conteiner)



wsl --shutdown            (para desligar a maquina virtual ubuntu)


Download docker - https://docs.docker.com/get-docker/
Docker hub - https://hub.docker.com/_/mysql
DBeaver dowload - https://dbeaver.io/download/
